<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    현재 환율 : <span id="pricePerDollar"></span>(<span id="datetime"></span>)
    <form action="/buy" method="post">
      <input type="text" id="amount" placeholder="구매수량" />
      <input type="text" id="totalPrice" disabled />
      <button>사기</button>
    </form>
    <script>
      let currentPrice = 1300;
      const amountInput = document.querySelector("#amount");

      amountInput.addEventListener("keyup", (e) => {
        console.log(1);
        const amount = e.target.value;
        document.querySelector("#totalPrice").value = amount * currentPrice;
      });
    </script>
  </body>
</html>
